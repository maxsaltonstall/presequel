# SQL Learning Game — Milestone C2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship Chapter 4 "The Robber Baron's Census" — the first chapter driven by a real(istic) external dataset. Player solves 6 word-bank puzzles teaching `COUNT`, `COUNT DISTINCT`, `SUM`, `AVG`, `MIN`, `MAX`, and `GROUP BY` against a ~3000-row synthetic 1890 NYC census CSV.

**Architecture:** Content-additive. No engine changes. The chapter's `seed.sql` uses DuckDB's `read_csv_auto` to materialize the external CSV into an in-memory `census_1890` table during chapter initialization — before the security hardening disables `read_csv_auto` for player queries.

**Tech Stack:** unchanged from C1.

**Deviations from spec:**
- Spec called for Parquet. This milestone uses CSV: author-friendly, git-diffable, read natively by DuckDB, same pedagogy. Parquet can be swapped in later without touching engine code.
- Spec called for "real historical data." This milestone uses synthetic data whose shape mirrors 1890 NYC census records. Real data can be swapped in later the same way (drop a CSV into the chapter folder).

**What ships at the end of Milestone C2:**
- Chapter 4 playable end-to-end from Chapter 3's Next button.
- ~3000-row `census_1890` CSV generated by a committed script and committed alongside the seed.
- 6 new puzzles teaching aggregation + `GROUP BY`.
- Cornelius Grayson speaks in bubbles.
- 4 new reference entries: `COUNT`, `SUM / AVG`, `MIN / MAX`, `GROUP BY`.
- Hemiunu surfaces as a single NULL-borough row — aggregation reveals the outlier.
- Playwright smoke extended to Ch4.

---

## The CSV path resolution trap (one-time concern)

DuckDB resolves relative file paths from the process's current working directory. `npm start` runs the server from the project root, so a seed like `read_csv_auto('content/chapters/04-census/census_1890.csv')` will work during server runtime.

For tests (`npm test`, Playwright's spawned server, content validator), cwd is also the project root. So no path-resolution special case is needed.

Document the constraint: **the server must be started from `/Users/max.saltonstall/sqllearning/`**. That's already how the README tells people to run it.

---

## File Structure

Files created:
- `scripts/generate-census-csv.js` — deterministic generator for the CSV
- `content/chapters/04-census/census_1890.csv` — committed generated output (~3000 rows)
- `content/chapters/04-census/chapter.json`
- `content/chapters/04-census/seed.sql`
- `content/chapters/04-census/puzzles/01.json` through `06.json`
- `content/reference/count.md`
- `content/reference/sum-avg.md`
- `content/reference/min-max.md`
- `content/reference/group-by.md`

Files modified:
- `src/main.js` — add `04-census` to `CHAPTER_ORDER`
- `src/dialogue.js` — add `grayson` to `SPEAKERS`
- `src/reference.js` — add `04-census` to `CONCEPTS_FOR_CHAPTER`
- `tests/e2e-smoke.spec.js` — Ch4 smoke
- `docs/playtest-checklist.md` — Ch4 section

---

## Phase 0 — Data

### Task 1: CSV generator + committed dataset

**Files:**
- Create: `scripts/generate-census-csv.js`
- Create: `content/chapters/04-census/census_1890.csv`

- [ ] **Step 1: Write the generator**

Create `scripts/generate-census-csv.js`:

```js
#!/usr/bin/env node
// Generate a deterministic synthetic 1890 NYC census CSV.
// Seeded PRNG so the output is identical on every run.

import { writeFile, mkdir } from 'node:fs/promises';
import { resolve } from 'node:path';
import { fileURLToPath } from 'node:url';

const __root = resolve(fileURLToPath(new URL('.', import.meta.url)), '..');
const OUT = resolve(__root, 'content', 'chapters', '04-census', 'census_1890.csv');
const TOTAL_ROWS = 3000;

// Deterministic linear-congruential PRNG.
let rngState = 1729;
function rand() {
  rngState = (rngState * 1103515245 + 12345) & 0x7fffffff;
  return rngState / 0x7fffffff;
}
function pick(arr) { return arr[Math.floor(rand() * arr.length)]; }
function pickWeighted(arr, weights) {
  const total = weights.reduce((a, b) => a + b, 0);
  let r = rand() * total;
  for (let i = 0; i < arr.length; i++) {
    r -= weights[i];
    if (r <= 0) return arr[i];
  }
  return arr[arr.length - 1];
}
function intBetween(lo, hi) { return lo + Math.floor(rand() * (hi - lo + 1)); }

const SURNAMES = [
  'O\'Brien','Murphy','Sullivan','Kelly','Ryan','Walsh','McCarthy','Callahan',
  'Schmidt','Mueller','Becker','Fischer','Weber','Hoffmann','Klein','Wagner',
  'Rossi','Ferrari','Russo','Esposito','Bianchi','Ricci','Conti','Marino',
  'Cohen','Levy','Goldberg','Katz','Friedman','Stein','Roth','Silverman',
  'Smith','Johnson','Williams','Brown','Jones','Miller','Davis','Wilson',
  'Kowalski','Novak','Wojcik','Lewandowski',
  'Andersen','Johansson','Petrov','Ivanov',
];
const FIRST_NAMES_M = [
  'John','James','Michael','William','Patrick','Thomas','Daniel','Joseph',
  'Heinrich','Friedrich','Karl','Otto','Wilhelm',
  'Giovanni','Giuseppe','Antonio','Luigi','Salvatore',
  'Isaac','Abraham','Samuel','David','Jacob',
  'Stanislaw','Josef','Boris',
];
const FIRST_NAMES_F = [
  'Mary','Bridget','Catherine','Margaret','Ellen','Sarah','Anna','Elizabeth',
  'Gertrud','Hilda','Marta','Greta',
  'Maria','Rosa','Angela','Teresa',
  'Rachel','Rebecca','Esther','Ruth','Leah',
  'Olga','Zofia',
];
const OCCUPATIONS = [
  'laborer','servant','seamstress','carpenter','clerk','teacher','factory worker',
  'merchant','bookkeeper','tailor','cobbler','baker','butcher','grocer',
  'engineer','machinist','porter','washerwoman','typesetter','driver',
  'nurse','shopkeeper','milliner','stevedore','none',
];
const BOROUGHS = ['Manhattan','Brooklyn','Bronx','Queens','Staten Island'];
const BOROUGH_WEIGHTS = [58, 30, 5, 5, 2]; // Manhattan-dominant per 1890 demography
const BIRTHPLACES = [
  'New York','Ireland','Germany','Italy','Russia','England','Poland','Scotland',
  'Austria-Hungary','Sweden','Norway','France','New Jersey','Massachusetts','Pennsylvania',
];
const BIRTHPLACE_WEIGHTS = [34, 22, 18, 10, 6, 3, 3, 1, 1, 1, 1, 1, 3, 2, 1];
const MARITAL = ['single','married','widowed','divorced'];
const MARITAL_WEIGHTS = [45, 45, 9, 1];

const rows = [];
let householdSeq = 1;
let i = 1;

while (i <= TOTAL_ROWS - 1) {
  // Generate a household of 1-5 residents sharing a surname and household_id.
  const surname = pick(SURNAMES);
  const size = intBetween(1, 5);
  const borough = pickWeighted(BOROUGHS, BOROUGH_WEIGHTS);
  const birthplace = pickWeighted(BIRTHPLACES, BIRTHPLACE_WEIGHTS);
  const household_id = householdSeq++;

  for (let k = 0; k < size && i <= TOTAL_ROWS - 1; k++) {
    const sex = rand() < 0.5 ? 'M' : 'F';
    const fn = pick(sex === 'M' ? FIRST_NAMES_M : FIRST_NAMES_F);
    // Age skews young: median ~25 with a long tail.
    const age = Math.min(95, Math.max(0, Math.floor(Math.pow(rand(), 1.5) * 80)));
    const marital = age < 16 ? 'single' : pickWeighted(MARITAL, MARITAL_WEIGHTS);
    const occ = (age < 12 || (age < 16 && rand() < 0.5)) ? 'none' : pick(OCCUPATIONS);
    // Wages correlate loosely with occupation and age.
    const baseWage = {
      'engineer': 90000, 'teacher': 55000, 'bookkeeper': 60000, 'clerk': 45000,
      'merchant': 120000, 'shopkeeper': 70000, 'nurse': 40000, 'tailor': 38000,
      'typesetter': 42000, 'machinist': 48000, 'carpenter': 45000, 'baker': 35000,
      'butcher': 37000, 'grocer': 40000, 'driver': 32000, 'porter': 28000,
      'factory worker': 30000, 'milliner': 30000, 'seamstress': 24000,
      'washerwoman': 20000, 'servant': 18000, 'cobbler': 33000, 'stevedore': 35000,
      'laborer': 26000, 'none': 0,
    }[occ] || 25000;
    const wage = Math.max(0, baseWage + intBetween(-5000, 15000));

    rows.push([i, surname, fn, age, sex, marital, occ, borough, birthplace, household_id, wage]);
    i++;
  }
}

// Row 3000: THE HEMIUNU ANOMALY.
// No borough, no occupation, no birthplace. Surname 'Hemiunu'. Single, age 43.
rows.push([3000, 'Hemiunu', 'H.', 43, 'M', 'single', null, null, null, null, null]);

// CSV writer — quote fields that contain commas or single quotes.
function csvCell(v) {
  if (v === null || v === undefined) return '';
  const s = String(v);
  if (s.includes(',') || s.includes('"') || s.includes('\n') || s.includes("'")) {
    return '"' + s.replace(/"/g, '""') + '"';
  }
  return s;
}

const HEADER = 'id,surname,first_name,age,sex,marital_status,occupation,borough,birthplace,household_id,annual_wage_cents';
const body = rows.map(r => r.map(csvCell).join(',')).join('\n');

await mkdir(resolve(__root, 'content', 'chapters', '04-census'), { recursive: true });
await writeFile(OUT, HEADER + '\n' + body + '\n', 'utf8');
console.log(`Wrote ${rows.length} rows to ${OUT}`);
```

- [ ] **Step 2: Run the generator**

Run: `node scripts/generate-census-csv.js`
Expected: prints `Wrote 3000 rows to .../census_1890.csv`. The CSV should have 3001 lines (1 header + 3000 data rows). The last row has surname "Hemiunu" with nulls for borough/occupation/birthplace/household_id/annual_wage_cents.

Verify:
```
wc -l content/chapters/04-census/census_1890.csv
grep Hemiunu content/chapters/04-census/census_1890.csv
```

The first should show 3001 lines. The second should show exactly one match.

- [ ] **Step 3: Commit**

```bash
git add scripts/generate-census-csv.js content/chapters/04-census/census_1890.csv
git commit -m "$(cat <<'EOF'
Add census CSV generator and generated 3000-row dataset

Deterministic (seeded PRNG) synthetic 1890 NYC census with realistic
demographic shape: Manhattan-dominant, immigrant-heavy. One anomalous
row (id=3000, surname=Hemiunu) has null borough/occupation/birthplace
— the aggregation puzzles reveal it via GROUP BY borough.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 1 — Engine glue

### Task 2: Engine glue for Chapter 4

**Files:**
- Modify: `src/main.js`, `src/dialogue.js`, `src/reference.js`

- [ ] **Step 1: Add `04-census` to CHAPTER_ORDER in src/main.js**

Change:
```js
const CHAPTER_ORDER = ['01-onboarding', '02-pharaoh', '03-speakeasy'];
```
to:
```js
const CHAPTER_ORDER = ['01-onboarding', '02-pharaoh', '03-speakeasy', '04-census'];
```

- [ ] **Step 2: Add `grayson` to SPEAKERS in src/dialogue.js**

Inside the existing SPEAKERS object, add:
```js
  grayson: { label: 'Cornelius Grayson', role: 'client' },
```

- [ ] **Step 3: Extend CONCEPTS_FOR_CHAPTER in src/reference.js**

Add this entry:
```js
  '04-census': [
    { slug: 'select', title: 'SELECT' },
    { slug: 'from',   title: 'FROM' },
    { slug: 'limit',  title: 'LIMIT' },
    { slug: 'where',  title: 'WHERE' },
    { slug: 'comparison-operators', title: 'Comparison ops' },
    { slug: 'order-by', title: 'ORDER BY' },
    { slug: 'like',    title: 'LIKE' },
    { slug: 'is-null', title: 'IS NULL' },
    { slug: 'string-functions', title: 'String functions' },
    { slug: 'count', title: 'COUNT' },
    { slug: 'sum-avg', title: 'SUM / AVG' },
    { slug: 'min-max', title: 'MIN / MAX' },
    { slug: 'group-by', title: 'GROUP BY' },
  ],
```

- [ ] **Step 4: Commit**

```bash
git add src/main.js src/dialogue.js src/reference.js
git commit -m "$(cat <<'EOF'
Wire Chapter 4 into engine: CHAPTER_ORDER, SPEAKERS, reference concepts

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 2 — Content

### Task 3: Chapter 4 seed, metadata, and reference markdown

**Files:**
- Create: `content/chapters/04-census/chapter.json`
- Create: `content/chapters/04-census/seed.sql`
- Create: `content/reference/count.md`
- Create: `content/reference/sum-avg.md`
- Create: `content/reference/min-max.md`
- Create: `content/reference/group-by.md`

Use the Write tool for every file.

- [ ] **Step 1: Create chapter.json**

```json
{
  "id": "04-census",
  "ordinal": 4,
  "title": "The Robber Baron's Census",
  "era": "NYC, 1890",
  "client": {
    "name": "Cornelius Grayson",
    "portrait": "grayson.svg",
    "voice": "imperious, impatient, monocle-energy"
  },
  "boss_intro": "Carol: 'Cornelius Grayson. Industrialist. He wants workforce stats by borough for an investor pitch Thursday. The data is a three-thousand-row census slice — real scale — so you're going to learn aggregation or die trying. COUNT, SUM, AVG, MIN, MAX, GROUP BY. Same word-bank mechanic.'",
  "concepts_introduced": ["count", "sum-avg", "min-max", "group-by"],
  "concepts_reviewed": ["where", "is-null", "order-by"],
  "mechanic_mode": "word_bank",
  "arc_hook": "A workforce breakdown by borough surfaces a group of 1 — a resident with no borough recorded. Surname: Hemiunu. That's three timelines.",
  "puzzle_ids": ["01", "02", "03", "04", "05", "06"],
  "outro": "Grayson gets his charts. The investors will be satisfied. Carol reads your working papers and sits very still. 'Old Kingdom Egypt. 1920s Chicago. 1890 New York. Three different Hemiunus. All with wrong data in the wrong places. That's not a coincidence, kid. That's a pattern.' She picks up the phone. 'Get me the CEO.'"
}
```

- [ ] **Step 2: Create seed.sql**

```sql
-- Chapter 4: The Robber Baron's Census
-- Loads ~3000 rows of synthetic 1890 NYC census data from a committed CSV.
-- One anomalous row (surname Hemiunu, id 3000) has null borough/occupation —
-- revealed by the aggregation puzzles.

CREATE TABLE census_1890 AS
  SELECT * FROM read_csv_auto('content/chapters/04-census/census_1890.csv');
```

- [ ] **Step 3: Create reference/count.md**

```markdown
---
concept: count
title: COUNT
introduced_in: 04-census
---

# COUNT

`COUNT` returns the number of rows. It's an **aggregate** — it collapses many rows into one answer.

## Syntax
```
SELECT COUNT(*) FROM table
SELECT COUNT(column) FROM table                 -- counts non-null values in column
SELECT COUNT(DISTINCT column) FROM table        -- counts distinct non-null values
```

## Examples

How many residents total?
```
SELECT COUNT(*) FROM census_1890
```

How many residents had a borough recorded (non-null)?
```
SELECT COUNT(borough) FROM census_1890
```

How many distinct occupations appear?
```
SELECT COUNT(DISTINCT occupation) FROM census_1890
```

## Gotchas

- `COUNT(*)` counts every row, even if every column is NULL.
- `COUNT(column)` skips NULL values in that column.
```

- [ ] **Step 4: Create reference/sum-avg.md**

```markdown
---
concept: sum-avg
title: SUM / AVG
introduced_in: 04-census
---

# SUM and AVG

Two more aggregates: `SUM` adds up numbers, `AVG` averages them.

## Syntax
```
SELECT SUM(column) FROM table
SELECT AVG(column) FROM table
```

Both skip NULL values automatically.

## Examples

Total wages paid across the whole census:
```
SELECT SUM(annual_wage_cents) FROM census_1890
```

Average age:
```
SELECT AVG(age) FROM census_1890
```

## Combining aggregates

You can compute multiple aggregates in one `SELECT`:
```
SELECT SUM(annual_wage_cents), AVG(age), COUNT(*) FROM census_1890
```
```

- [ ] **Step 5: Create reference/min-max.md**

```markdown
---
concept: min-max
title: MIN / MAX
introduced_in: 04-census
---

# MIN and MAX

Aggregates that find the smallest / largest value in a column. Work on numbers, strings (alphabetical), and dates.

## Syntax
```
SELECT MIN(column) FROM table
SELECT MAX(column) FROM table
```

Both skip NULL values.

## Examples

Youngest and oldest residents:
```
SELECT MIN(age), MAX(age) FROM census_1890
```

Smallest and largest wage on record:
```
SELECT MIN(annual_wage_cents), MAX(annual_wage_cents) FROM census_1890
```

Alphabetically first surname:
```
SELECT MIN(surname) FROM census_1890
```
```

- [ ] **Step 6: Create reference/group-by.md**

```markdown
---
concept: group-by
title: GROUP BY
introduced_in: 04-census
---

# GROUP BY

`GROUP BY` splits rows into buckets, then lets you run aggregates (COUNT, SUM, AVG, MIN, MAX) per bucket instead of over the whole table.

## Syntax
```
SELECT bucket_column, AGG(other_column)
FROM table
GROUP BY bucket_column
```

## Examples

Residents per borough:
```
SELECT borough, COUNT(*)
FROM census_1890
GROUP BY borough
```

Returns one row per distinct borough. If any rows have `borough = NULL`, they form their own group with `NULL` as the bucket key.

Average age per borough, sorted by population:
```
SELECT borough, AVG(age), COUNT(*)
FROM census_1890
GROUP BY borough
ORDER BY COUNT(*) DESC
```

## Gotchas

- Every non-aggregate column in the `SELECT` must appear in `GROUP BY`.
- `NULL` becomes its own group — sometimes that's revealing.
```

- [ ] **Step 7: Validate**

Run: `npm run validate-content`
Expected: "Content valid: all chapters and puzzles pass." (No puzzles for Ch4 yet — this only validates the chapter.json schema and that the seed runs.)

**If the seed fails** with an error about `read_csv_auto` being disabled, there's a bug in the hardening sequence. Check `server/duckdb.js openChapter`: the `SET enable_external_access = false` must come AFTER the seed runs. It should already be correct — this is just a sanity check if things break.

**If the seed fails** with "file not found," the CSV path is wrong. Confirm `npm start` works from the project root and that `content/chapters/04-census/census_1890.csv` exists.

- [ ] **Step 8: Commit**

```bash
git add content/chapters/04-census/chapter.json content/chapters/04-census/seed.sql content/reference/count.md content/reference/sum-avg.md content/reference/min-max.md content/reference/group-by.md
git commit -m "$(cat <<'EOF'
Add Chapter 4 seed, metadata, and 4 new reference markdown files

The seed does CREATE TABLE ... AS SELECT * FROM read_csv_auto(...) —
runs once during chapter init, before DuckDB hardening disables
external file access for player queries.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Chapter 4 puzzles 01–06

Six word-bank puzzles building toward composite `GROUP BY`:

1. `COUNT(*)` — total rows
2. `COUNT(DISTINCT occupation)` — distinct occupations
3. `SUM(annual_wage_cents)` — total annual wages paid
4. `MIN(age)`, `MAX(age)`, `AVG(age)` — combined aggregates in one SELECT
5. `GROUP BY borough COUNT(*)` with `ORDER BY COUNT(*) DESC` — residents per borough. The `NULL` bucket surfaces.
6. Composite — `GROUP BY borough` with `COUNT(*)`, `AVG(age)`, `MAX(age)` — the investor-pitch deliverable; the NULL row's single data point is dramatically visible.

**Files:**
- Create: `content/chapters/04-census/puzzles/01.json` through `06.json`

- [ ] **Step 1: Puzzle 01 — COUNT(*)**

```json
{
  "id": "01",
  "concept": "count",
  "brief": {
    "speaker": "grayson",
    "text": "Start with the total, clerk. How many souls are in this census? A number. One number. That's all."
  },
  "template": [
    { "type": "keyword", "text": "SELECT" },
    { "type": "blank",   "id": "agg",  "mode": "word_bank",
      "options": ["COUNT(*)", "SUM(*)", "AVG(*)"] },
    { "type": "keyword", "text": "FROM" },
    { "type": "blank",   "id": "tbl",  "mode": "word_bank",
      "options": ["census_1890", "residents", "people"] }
  ],
  "expected": {
    "method": "rows",
    "sql": "SELECT COUNT(*) FROM census_1890",
    "order_sensitive": false
  },
  "hints": [
    { "when": "error",   "text": "Grayson: 'Your SQL is nonsense. Use COUNT with a star for the total.'" },
    { "when": "default", "text": "Grayson: 'Total rows. The aggregate function for that is COUNT.'" }
  ],
  "success": {
    "speaker": "grayson",
    "text": "Three thousand. A modest borough's worth. Onward."
  }
}
```

- [ ] **Step 2: Puzzle 02 — COUNT(DISTINCT occupation)**

```json
{
  "id": "02",
  "concept": "count",
  "brief": {
    "speaker": "grayson",
    "text": "How many distinct occupations does this borough support? Investors love that number. Diversity of labor, they call it. Count the unique occupations."
  },
  "template": [
    { "type": "keyword", "text": "SELECT" },
    { "type": "blank",   "id": "agg",  "mode": "word_bank",
      "options": ["COUNT(DISTINCT occupation)", "COUNT(occupation)", "COUNT(*)"] },
    { "type": "keyword", "text": "FROM" },
    { "type": "text",    "text": "census_1890" }
  ],
  "expected": {
    "method": "rows",
    "sql": "SELECT COUNT(DISTINCT occupation) FROM census_1890",
    "order_sensitive": false
  },
  "hints": [
    { "when": "different-values", "text": "Grayson: 'Close, but you're including NULLs or duplicates. DISTINCT. The word is DISTINCT.'" },
    { "when": "default",          "text": "Grayson: 'Distinct occupations. Not all three-thousand rows.'" }
  ],
  "success": {
    "speaker": "grayson",
    "text": "Twenty-five distinct trades. Impressive number for a pitch. Put it in the deck."
  }
}
```

- [ ] **Step 3: Puzzle 03 — SUM(annual_wage_cents)**

```json
{
  "id": "03",
  "concept": "sum-avg",
  "brief": {
    "speaker": "grayson",
    "text": "Total wages paid across this workforce. All of it. In cents, because that's how my accountants like it."
  },
  "template": [
    { "type": "keyword", "text": "SELECT" },
    { "type": "blank",   "id": "agg",  "mode": "word_bank",
      "options": ["SUM(annual_wage_cents)", "AVG(annual_wage_cents)", "COUNT(annual_wage_cents)"] },
    { "type": "keyword", "text": "FROM" },
    { "type": "text",    "text": "census_1890" }
  ],
  "expected": {
    "method": "rows",
    "sql": "SELECT SUM(annual_wage_cents) FROM census_1890",
    "order_sensitive": false
  },
  "hints": [
    { "when": "different-values", "text": "Grayson: 'That's not the total. SUM adds. AVG averages.'" },
    { "when": "default",          "text": "Grayson: 'SUM. Of the wage column. Single row, single number.'" }
  ],
  "success": {
    "speaker": "grayson",
    "text": "Nearly two hundred million cents a year. The economy of a small city. Good."
  }
}
```

- [ ] **Step 4: Puzzle 04 — MIN, MAX, AVG age combined**

```json
{
  "id": "04",
  "concept": "min-max",
  "brief": {
    "speaker": "grayson",
    "text": "The age profile. Give me the minimum, maximum, and average age of the workforce. Three numbers. One query."
  },
  "template": [
    { "type": "keyword", "text": "SELECT" },
    { "type": "blank",   "id": "a1",  "mode": "word_bank",
      "options": ["MIN(age)", "MAX(age)", "SUM(age)"] },
    { "type": "text",    "text": "," },
    { "type": "blank",   "id": "a2",  "mode": "word_bank",
      "options": ["MAX(age)", "MIN(age)", "AVG(age)"] },
    { "type": "text",    "text": "," },
    { "type": "blank",   "id": "a3",  "mode": "word_bank",
      "options": ["AVG(age)", "COUNT(age)", "MIN(age)"] },
    { "type": "keyword", "text": "FROM" },
    { "type": "text",    "text": "census_1890" }
  ],
  "expected": {
    "method": "rows",
    "sql": "SELECT MIN(age), MAX(age), AVG(age) FROM census_1890",
    "order_sensitive": false
  },
  "hints": [
    { "when": "different-values", "text": "Grayson: 'Values don't match. Min, max, and average — in some order.'" },
    { "when": "error",            "text": "Grayson: 'Three aggregate expressions separated by commas.'" },
    { "when": "default",          "text": "Grayson: 'Pick MIN, MAX, and AVG — each on the age column.'" }
  ],
  "success": {
    "speaker": "grayson",
    "text": "Youngest a newborn. Oldest in the nineties. Average around thirty. The investors like numbers."
  }
}
```

- [ ] **Step 5: Puzzle 05 — GROUP BY borough (anomaly surfaces)**

```json
{
  "id": "05",
  "concept": "group-by",
  "brief": {
    "speaker": "grayson",
    "text": "Workforce by borough. One row per borough with a count. Sorted so the largest borough is at the top. This is the chart they want to see."
  },
  "template": [
    { "type": "keyword", "text": "SELECT" },
    { "type": "blank",   "id": "col1", "mode": "word_bank",
      "options": ["borough", "occupation", "sex"] },
    { "type": "text",    "text": "," },
    { "type": "blank",   "id": "agg",  "mode": "word_bank",
      "options": ["COUNT(*)", "SUM(*)", "AVG(*)"] },
    { "type": "keyword", "text": "FROM" },
    { "type": "text",    "text": "census_1890" },
    { "type": "keyword", "text": "GROUP BY" },
    { "type": "blank",   "id": "grp",  "mode": "word_bank",
      "options": ["borough", "occupation", "sex"] },
    { "type": "keyword", "text": "ORDER BY" },
    { "type": "blank",   "id": "ord",  "mode": "word_bank",
      "options": ["COUNT(*)", "borough", "AVG(age)"] },
    { "type": "blank",   "id": "dir",  "mode": "word_bank",
      "options": ["DESC", "ASC"] }
  ],
  "expected": {
    "method": "rows",
    "sql": "SELECT borough, COUNT(*) FROM census_1890 GROUP BY borough ORDER BY COUNT(*) DESC",
    "order_sensitive": true
  },
  "hints": [
    { "when": "wrong_count_high", "text": "Grayson: 'Too many rows. Are you grouping correctly?'" },
    { "when": "different-values", "text": "Grayson: 'Right structure, wrong order or columns. Boroughs grouped, COUNT(*) ordered descending.'" },
    { "when": "error",            "text": "Grayson: 'Every non-aggregate in SELECT must appear in GROUP BY.'" },
    { "when": "default",          "text": "Grayson: 'SELECT borough, COUNT — GROUP BY borough — ORDER BY COUNT descending.'" }
  ],
  "success": {
    "speaker": "grayson",
    "text": "Manhattan leads. Brooklyn second. Bronx, Queens, Staten Island trailing. And... there's a sixth row. A null borough. One resident. How? I do not sign off on data anomalies. Find out who that is."
  }
}
```

- [ ] **Step 6: Puzzle 06 — Composite aggregation (finale)**

```json
{
  "id": "06",
  "concept": "group-by",
  "brief": {
    "speaker": "grayson",
    "text": "Final deliverable. For each borough: the count, the average age, and the oldest resident. One query. Ordered so Manhattan leads. Include the anomalous NULL bucket — investors deserve transparency."
  },
  "template": [
    { "type": "keyword", "text": "SELECT" },
    { "type": "text",    "text": "borough," },
    { "type": "blank",   "id": "a1",  "mode": "word_bank",
      "options": ["COUNT(*)", "SUM(*)", "MAX(*)"] },
    { "type": "text",    "text": "," },
    { "type": "blank",   "id": "a2",  "mode": "word_bank",
      "options": ["AVG(age)", "MIN(age)", "SUM(age)"] },
    { "type": "text",    "text": "," },
    { "type": "blank",   "id": "a3",  "mode": "word_bank",
      "options": ["MAX(age)", "MIN(age)", "COUNT(age)"] },
    { "type": "keyword", "text": "FROM" },
    { "type": "text",    "text": "census_1890" },
    { "type": "keyword", "text": "GROUP BY" },
    { "type": "text",    "text": "borough" },
    { "type": "keyword", "text": "ORDER BY" },
    { "type": "blank",   "id": "ord", "mode": "word_bank",
      "options": ["COUNT(*)", "AVG(age)", "borough"] },
    { "type": "blank",   "id": "dir", "mode": "word_bank",
      "options": ["DESC", "ASC"] }
  ],
  "expected": {
    "method": "rows",
    "sql": "SELECT borough, COUNT(*), AVG(age), MAX(age) FROM census_1890 GROUP BY borough ORDER BY COUNT(*) DESC",
    "order_sensitive": true
  },
  "hints": [
    { "when": "different-values", "text": "Grayson: 'Structure's right, some column's wrong. Count, average age, max age. In that order. Ordered by count, largest first.'" },
    { "when": "error",            "text": "Grayson: 'Syntax. Commas between aggregates.'" },
    { "when": "default",          "text": "Grayson: 'Six rows. Five boroughs plus the anomaly. Each row: count, avg age, max age.'" }
  ],
  "success": {
    "speaker": "grayson",
    "text": "Beautiful. Thursday's pitch is made. The NULL row, though — one resident, age 43, alone in their own bucket. Surname Hemiunu. I have... heard this name recently. From a colleague. In very different circumstances. Take your fee, clerk. And when you see your superior, tell her we should speak."
  }
}
```

- [ ] **Step 7: Validate**

Run: `npm run validate-content`
Expected: "Content valid: all chapters and puzzles pass."

If any puzzle's `expected.sql` fails to execute, STOP and report BLOCKED with the exact error — do not adjust expected SQL to match a broken query.

- [ ] **Step 8: Commit**

```bash
git add content/chapters/04-census/puzzles/
git commit -m "$(cat <<'EOF'
Add Chapter 4 puzzles 01-06 (aggregation + GROUP BY)

Progression: COUNT(*), COUNT DISTINCT, SUM, MIN/MAX/AVG composite,
GROUP BY borough with ORDER BY (surfaces NULL anomaly), composite
GROUP BY with three aggregates (the investor-pitch deliverable).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 3 — Tests + ship

### Task 5: Playwright Ch4 smoke

**Files:**
- Modify: `tests/e2e-smoke.spec.js`

- [ ] **Step 1: Append test**

```js
test('Chapter 4 aggregation puzzle can be solved', async ({ page }) => {
  await page.goto('/');
  await page.evaluate(() => localStorage.clear());
  await page.reload();

  await page.evaluate(() => {
    localStorage.setItem('chronoConsultingState-v1', JSON.stringify({
      currentChapterId: '04-census',
      currentPuzzleId: '01',
      chapters: {
        '01-onboarding': { completed: true, solved: ['01','02','03','04','05'], attempts: {} },
        '02-pharaoh':    { completed: true, solved: ['01','02','03','04','05','06'], attempts: {} },
        '03-speakeasy':  { completed: true, solved: ['01','02','03','04','05','06'], attempts: {} },
      },
      referenceOpened: [],
      savedAt: Date.now(),
    }));
  });
  await page.reload();

  // Boss intro + Grayson brief
  await expect(page.locator('.bubble')).toHaveCount(2, { timeout: 5000 });
  await expect(page.locator('.bubble').last()).toContainText('souls');

  // Word bank with COUNT(*) + other chips
  const chips = page.locator('.word-bank-chip');
  await expect(chips.first()).toBeVisible();

  // Fill: COUNT(*) then census_1890
  await chips.filter({ hasText: /^COUNT\(\*\)$/ }).click();
  await chips.filter({ hasText: /^census_1890$/ }).click();

  await expect(page.locator('.slot.empty')).toHaveCount(0);

  await page.locator('#run-btn').click();

  await expect(page.locator('.bubble.success')).toHaveCount(1, { timeout: 5000 });
});
```

- [ ] **Step 2: Run**

Run: `npm run test:e2e`
Expected: 5/5 pass.

- [ ] **Step 3: Commit**

```bash
git add tests/e2e-smoke.spec.js
git commit -m "$(cat <<'EOF'
Extend Playwright smoke to cover Chapter 4 aggregation puzzle

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: Playtest checklist update

**Files:**
- Modify: `docs/playtest-checklist.md`

- [ ] **Step 1: Append Ch4 section**

Insert after the Ch3 reference-drawer section, before "Security spot-check":

```markdown
## Chapter 4 — The Robber Baron's Census (aggregation + GROUP BY)

### Transition
- [ ] After finishing Ch3, the game auto-advances to Ch4 Puzzle 01.
- [ ] First-load delay: the chapter's seed.sql loads ~3000 rows from the CSV. Under ~2 seconds. No visible flicker beyond normal chapter load.

### Puzzle 01 (COUNT)
- [ ] `COUNT(*)` returns 3000.
- [ ] Picking `SUM(*)` or `AVG(*)` errors → hint.

### Puzzle 02 (COUNT DISTINCT)
- [ ] `COUNT(DISTINCT occupation)` returns 25 (or however many the generator produced).

### Puzzle 03 (SUM)
- [ ] `SUM(annual_wage_cents)` returns ~2 × 10^8. Big number.

### Puzzle 04 (MIN/MAX/AVG)
- [ ] Three-aggregate SELECT returns one row with MIN, MAX, AVG of age.
- [ ] Ordering mistakes (MIN instead of MAX) trigger different-values hints.

### Puzzle 05 (GROUP BY borough — anomaly)
- [ ] Returns 6 rows: 5 boroughs + NULL.
- [ ] Manhattan leads by a wide margin.
- [ ] The NULL bucket has count = 1. Success text calls it out.

### Puzzle 06 (Composite GROUP BY)
- [ ] Returns 6 rows with borough, count, avg(age), max(age).
- [ ] NULL row: borough=NULL, count=1, avg_age=43, max_age=43.
- [ ] Outro: Grayson names Hemiunu; Carol connects all three timelines.

## Reference drawer — Chapter 4
- [ ] Drawer shows 13 concepts.
- [ ] COUNT / SUM / AVG / MIN / MAX / GROUP BY all render without error.
```

- [ ] **Step 2: Commit**

```bash
git add docs/playtest-checklist.md
git commit -m "$(cat <<'EOF'
Extend playtest checklist with Chapter 4 manual steps

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: Final verification + milestone-c2 tag

- [ ] **Step 1: Full test matrix**

```bash
npm test
npm run validate-content
npm run test:e2e
```

All three should pass.

- [ ] **Step 2: Tag**

```bash
git tag -a milestone-c2 -m "Milestone C2: Chapter 4 playable — aggregation + GROUP BY with real-scale census data"
```

Do NOT push.

---

## Definition of Done

- [ ] All 7 tasks checked off.
- [ ] `npm test` — all green.
- [ ] `npm run validate-content` passes across Ch1–4.
- [ ] `npm run test:e2e` — all green (5 smokes).
- [ ] `milestone-c2` tag created locally.

---

## Out of scope for Milestone C2

- `HAVING` clause (deferred to later chapters).
- Subqueries / CTEs (Phase 1.5 or Milestone F).
- Typing mechanic (Milestone D: Chapter 5).
- Swapping in real historical data (cosmetic — drop a real CSV into the chapter folder with matching columns).
