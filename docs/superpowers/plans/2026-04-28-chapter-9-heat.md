# Chapter 9 "Heat" Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship Chapter 9 "Heat" — a pre-aggregated metrics chapter teaching `rate(field, 1m)` to convert per-minute counts into events per second, with chrono-portal-mirror's 18-minute burst pattern as the discovery arc.

**Architecture:** New DDSQL translator function `translateRate` is added to `server/ddsql.js` and wired innermost in the pipeline (`translateBucket(translateTagFilter(translateTimeWindow(translateRate(sql))))`). Data is a pre-aggregated parquet file (720 rows: 6 services × 120 minutes) generated by a new script. Six puzzle JSON files plus seed.sql and chapter.json drive the game engine without any frontend changes.

**Tech Stack:** Node.js ESM, `@duckdb/node-api`, Playwright (E2E), `node:test` (unit tests), Parquet

---

## File Map

| Action | Path | Purpose |
|--------|------|---------|
| Create | `tests/ddsql-rate.test.js` | Unit tests for translateRate (9 tests) |
| Modify | `server/ddsql.js` | Add `translateRate` export |
| Modify | `server.js:6,123` | Import and wire `translateRate` |
| Create | `scripts/generate-ch9-metrics.js` | Parquet generator |
| Create | `content/chapters/09-heat/data/metrics.parquet` | Generated by script |
| Create | `content/chapters/09-heat/seed.sql` | DuckDB session setup |
| Create | `content/chapters/09-heat/chapter.json` | Chapter metadata |
| Create | `content/chapters/09-heat/puzzles/01.json` | Schema intro |
| Create | `content/chapters/09-heat/puzzles/02.json` | First rate() |
| Create | `content/chapters/09-heat/puzzles/03.json` | MAX per service |
| Create | `content/chapters/09-heat/puzzles/04.json` | Zoom into phantom |
| Create | `content/chapters/09-heat/puzzles/05.json` | Filter to spikes |
| Create | `content/chapters/09-heat/puzzles/06.json` | Error rate during spikes |
| Create | `content/reference/rate.md` | Rate reference doc |
| Modify | `src/main.js:9` | Append `'09-heat'` to CHAPTER_ORDER |
| Modify | `src/reference.js:78–88` | Add `'09-heat'` concepts entry |
| Modify | `tests/e2e-smoke.spec.js` | Append Ch9 smoke test |
| Modify | `docs/playtest-checklist.md` | Append Ch9 section |

---

## Task 1: translateRate — unit tests + implementation

**Files:**
- Create: `tests/ddsql-rate.test.js`
- Modify: `server/ddsql.js`
- Modify: `server.js`

- [ ] **Step 1: Write the failing tests**

Create `tests/ddsql-rate.test.js`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { translateRate, translateTagFilter } from '../server/ddsql.js';

test('rate(n, 1m) → ROUND(n / 60.0, 2)', () => {
  assert.equal(
    translateRate('SELECT rate(n, 1m) AS rps FROM metrics'),
    'SELECT ROUND(n / 60.0, 2) AS rps FROM metrics'
  );
});

test('rate(errors, 1m) → ROUND(errors / 60.0, 2)', () => {
  assert.equal(
    translateRate('SELECT rate(errors, 1m) AS err_rps FROM metrics'),
    'SELECT ROUND(errors / 60.0, 2) AS err_rps FROM metrics'
  );
});

test('case-insensitive: RATE(n, 1M) → ROUND(n / 60.0, 2)', () => {
  assert.equal(
    translateRate('SELECT RATE(n, 1M) AS rps FROM metrics'),
    'SELECT ROUND(n / 60.0, 2) AS rps FROM metrics'
  );
});

test('inside SELECT with alias: rate replaced correctly', () => {
  assert.equal(
    translateRate('SELECT service, rate(n, 1m) AS rps FROM metrics'),
    'SELECT service, ROUND(n / 60.0, 2) AS rps FROM metrics'
  );
});

test('inside WHERE: rate(n, 1m) > 1.0 → ROUND(n / 60.0, 2) > 1.0', () => {
  assert.equal(
    translateRate("SELECT minute FROM metrics WHERE service = 'x' AND rate(n, 1m) > 1.0"),
    "SELECT minute FROM metrics WHERE service = 'x' AND ROUND(n / 60.0, 2) > 1.0"
  );
});

test('inside aggregate: MAX(rate(n, 1m)) → MAX(ROUND(n / 60.0, 2))', () => {
  assert.equal(
    translateRate('SELECT service, MAX(rate(n, 1m)) AS peak FROM metrics GROUP BY service'),
    'SELECT service, MAX(ROUND(n / 60.0, 2)) AS peak FROM metrics GROUP BY service'
  );
});

test('no match: rate(n, 5m) → unchanged', () => {
  const sql = 'SELECT rate(n, 5m) FROM metrics';
  assert.equal(translateRate(sql), sql);
});

test('no match: rate(n) (no interval) → unchanged', () => {
  const sql = 'SELECT rate(n) FROM metrics';
  assert.equal(translateRate(sql), sql);
});

test('composition: translateRate output survives translateTagFilter unchanged', () => {
  const sql = "SELECT rate(n, 1m) AS rps FROM metrics WHERE service:auth-svc";
  assert.equal(
    translateTagFilter(translateRate(sql)),
    "SELECT ROUND(n / 60.0, 2) AS rps FROM metrics WHERE tags['service'] = 'auth-svc'"
  );
});
```

- [ ] **Step 2: Run tests — expect all 9 to fail**

```bash
node --test tests/ddsql-rate.test.js
```

Expected: 9 failures with `TypeError: translateRate is not a function`

- [ ] **Step 3: Add `translateRate` to `server/ddsql.js`**

Append to the end of `server/ddsql.js`:

```js
export function translateRate(sql) {
  return sql.replace(/\brate\s*\(\s*(\w+)\s*,\s*1m\s*\)/gi, (_, field) =>
    `ROUND(${field} / 60.0, 2)`
  );
}
```

- [ ] **Step 4: Run tests — expect all 9 to pass**

```bash
node --test tests/ddsql-rate.test.js
```

Expected: 9 passing

- [ ] **Step 5: Wire `translateRate` into `server.js`**

Change line 6 of `server.js`:
```js
// Before:
import { translateTagFilter, translateTimeWindow, translateBucket } from './server/ddsql.js';
// After:
import { translateTagFilter, translateTimeWindow, translateBucket, translateRate } from './server/ddsql.js';
```

Change line 123 of `server.js`:
```js
// Before:
const translated = translateBucket(translateTagFilter(translateTimeWindow(sql)));
// After:
const translated = translateBucket(translateTagFilter(translateTimeWindow(translateRate(sql))));
```

- [ ] **Step 6: Run full test suite to confirm no regressions**

```bash
npm test
```

Expected: all existing tests pass (85+)

- [ ] **Step 7: Commit**

```bash
git add tests/ddsql-rate.test.js server/ddsql.js server.js
git commit -m "feat(ddsql): add translateRate — rate(n, 1m) → ROUND(n / 60.0, 2)"
```

---

## Task 2: Data generator + parquet file

**Files:**
- Create: `scripts/generate-ch9-metrics.js`
- Create: `content/chapters/09-heat/data/metrics.parquet` (generated)

- [ ] **Step 1: Create `scripts/generate-ch9-metrics.js`**

```js
import { DuckDBInstance } from '@duckdb/node-api';
import { mkdirSync } from 'node:fs';
import { resolve, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const OUT_DIR  = resolve(__dirname, '..', 'content', 'chapters', '09-heat', 'data');
const OUT_PATH = resolve(OUT_DIR, 'metrics.parquet');

const START_MS = Date.UTC(2026, 3, 26, 10, 0, 0); // 2026-04-26 10:00:00 UTC
const MINUTES  = 120;

const QUIET_SERVICES = ['auth-svc', 'api-gateway', 'payment-svc', 'billing-svc', 'notification-svc'];
const PHANTOM_SVC    = 'chrono-portal-mirror';
const ALL_SERVICES   = [...QUIET_SERVICES, PHANTOM_SVC];

const SPIKE_OFFSETS = new Set([15, 33, 51, 69, 87, 105]);

function minuteOffset(minuteStr) {
  return (new Date(minuteStr.replace(' ', 'T') + 'Z').getTime() - START_MS) / 60_000;
}

function fmtMinute(m) {
  return new Date(START_MS + m * 60_000).toISOString()
    .replace('T', ' ').replace('Z', '').slice(0, 19);
}

function sqlStr(s) {
  return "'" + String(s).replace(/'/g, "''") + "'";
}

function getN(service, m) {
  if (service === PHANTOM_SVC && SPIKE_OFFSETS.has(m)) {
    return 150 + (m * 7 + service.length) % 51; // 150–200
  }
  if (service === PHANTOM_SVC) {
    return 2 + (m * 3 + 5) % 4; // 2–5
  }
  return 10 + (m * 11 + service.length * 7) % 21; // 10–30
}

function getErrors(service, m) {
  if (service === PHANTOM_SVC && SPIKE_OFFSETS.has(m)) {
    return 20 + (m * 5 + service.length) % 11; // 20–30
  }
  if (service === PHANTOM_SVC) return 0;
  return (m * 3 + service.length) % 3; // 0–2
}

function generateRows() {
  const rows = [];
  for (const service of ALL_SERVICES) {
    for (let m = 0; m < MINUTES; m++) {
      rows.push({ minute: fmtMinute(m), service, n: getN(service, m), errors: getErrors(service, m) });
    }
  }
  return rows;
}

async function main() {
  const rows = generateRows();

  if (rows.length !== 720)
    throw new Error(`Expected 720 rows, got ${rows.length}`);

  const phantomRows = rows.filter(r => r.service === PHANTOM_SVC);
  const spikeRows   = phantomRows.filter(r => SPIKE_OFFSETS.has(minuteOffset(r.minute)));
  if (spikeRows.length !== 6)
    throw new Error(`Expected 6 spike rows, got ${spikeRows.length}`);
  if (!spikeRows.every(r => r.n > 60))
    throw new Error('All spike rows must have n > 60');

  const quietPhantom = phantomRows.filter(r => !SPIKE_OFFSETS.has(minuteOffset(r.minute)));
  if (!quietPhantom.every(r => r.n <= 60))
    throw new Error('All non-spike phantom rows must have n <= 60');

  const START_STR = fmtMinute(0);
  const END_STR   = fmtMinute(MINUTES - 1);
  if (rows.some(r => r.minute < START_STR || r.minute > END_STR))
    throw new Error('Some rows fall outside the time window');

  const instance = await DuckDBInstance.create(':memory:');
  const conn     = await instance.connect();

  await conn.run(`
    CREATE TABLE metrics (
      minute  TIMESTAMP,
      service VARCHAR,
      n       INTEGER,
      errors  INTEGER
    )
  `);

  const BATCH = 200;
  for (let i = 0; i < rows.length; i += BATCH) {
    const batch = rows.slice(i, i + BATCH);
    const vals  = batch.map(r =>
      `(TIMESTAMP ${sqlStr(r.minute)}, ${sqlStr(r.service)}, ${r.n}, ${r.errors})`
    ).join(',\n');
    await conn.run(`INSERT INTO metrics VALUES\n${vals}`);
  }

  mkdirSync(OUT_DIR, { recursive: true });
  await conn.run(`COPY metrics TO ${sqlStr(OUT_PATH)} (FORMAT PARQUET)`);
  console.log(`Generated 720 rows → ${OUT_PATH}`);
  process.exit(0);
}

main().catch(err => { console.error(err); process.exit(1); });
```

- [ ] **Step 2: Run the generator**

```bash
node scripts/generate-ch9-metrics.js
```

Expected output: `Generated 720 rows → .../content/chapters/09-heat/data/metrics.parquet`

- [ ] **Step 3: Spot-check the parquet**

```bash
node --input-type=module <<'EOF'
import { DuckDBInstance } from '@duckdb/node-api';
const db = await DuckDBInstance.create(':memory:');
const conn = await db.connect();
const r = await conn.runAndReadAll("SELECT COUNT(*) AS n FROM read_parquet('content/chapters/09-heat/data/metrics.parquet')");
console.log('row count:', r.getRows()[0][0]);
const spike = await conn.runAndReadAll("SELECT minute, n FROM read_parquet('content/chapters/09-heat/data/metrics.parquet') WHERE service = 'chrono-portal-mirror' AND n > 60 ORDER BY minute");
console.log('spike rows:', spike.getRows().length, spike.getRows());
process.exit(0);
EOF
```

Expected: row count = 720, spike rows = 6

- [ ] **Step 4: Commit**

```bash
git add scripts/generate-ch9-metrics.js content/chapters/09-heat/data/metrics.parquet
git commit -m "feat(ch9): add metrics parquet generator and data file"
```

---

## Task 3: Content files — seed, chapter, puzzles

**Files:**
- Create: `content/chapters/09-heat/seed.sql`
- Create: `content/chapters/09-heat/chapter.json`
- Create: `content/chapters/09-heat/puzzles/01.json`
- Create: `content/chapters/09-heat/puzzles/02.json`
- Create: `content/chapters/09-heat/puzzles/03.json`
- Create: `content/chapters/09-heat/puzzles/04.json`
- Create: `content/chapters/09-heat/puzzles/05.json`
- Create: `content/chapters/09-heat/puzzles/06.json`

- [ ] **Step 1: Create `content/chapters/09-heat/seed.sql`**

```sql
SET VARIABLE ch9_anchor = TIMESTAMP '2026-04-26 12:00:00';
CREATE TABLE metrics AS
  SELECT * FROM read_parquet('${CONTENT_ROOT}/chapters/09-heat/data/metrics.parquet');
```

- [ ] **Step 2: Create `content/chapters/09-heat/chapter.json`**

```json
{
  "id": "09-heat",
  "ordinal": 9,
  "title": "Heat",
  "era": "Chrono Consulting HQ, present day",
  "client": {
    "name": "Carol",
    "portrait": "carol.svg",
    "voice": "dry, tired, weirdly protective"
  },
  "boss_intro": "M.'s plane landed at six. She hasn't called. Carol is looking at the metrics table — different shape than logs. Pre-aggregated. Every minute, every service: n is total events, errors is error events. No raw messages. No tags column. Just counts.\n\nRate is what you get when you divide a count by time. Rate is what M. asked for.",
  "concepts_introduced": ["rate"],
  "concepts_reviewed": ["select", "from", "where", "group-by", "order-by"],
  "mechanic_mode": "typing",
  "arc_hook": "Something fires every eighteen minutes. M. already knew which seconds. Now we need to know how fast.",
  "puzzle_ids": ["01", "02", "03", "04", "05", "06"],
  "outro": "Carol marks the six windows on the board. Eighteen minutes between each one. She writes the number in the gap and steps back.\n\nPre-aggregated metrics don't lie the way logs can. Logs get dropped. Counts are already compressed. Whatever filled this table ran on a schedule for at least three hours.\n\nM. was right about the seconds. Now she's asking what it was trying to do."
}
```

- [ ] **Step 3: Create `content/chapters/09-heat/puzzles/01.json`**

```json
{
  "id": "01",
  "concept": "metrics-schema",
  "brief": {
    "speaker": "carol",
    "text": "Different shape this time. No tags, no messages. Just counts by the minute. Show me ten rows."
  },
  "template": [
    { "type": "keyword", "text": "SELECT" },
    { "type": "text",    "text": "*" },
    { "type": "keyword", "text": "FROM" },
    { "type": "blank",   "id": "tbl", "mode": "typed", "placeholder": "table name" },
    { "type": "keyword", "text": "LIMIT" },
    { "type": "text",    "text": "10" }
  ],
  "expected": {
    "method": "rows",
    "sql": "SELECT * FROM metrics LIMIT 10",
    "order_sensitive": false
  },
  "hints": [
    { "when": "wrong_count_high", "text": "Carol: 'That's more than ten. LIMIT 10 is already in the query.'" },
    { "when": "wrong_count_low",  "text": "Carol: 'Empty result. The table is metrics.'" },
    { "when": "error",            "text": "Carol: 'Query error. The table is metrics — four columns: minute, service, n, errors.'" },
    { "when": "default",          "text": "Carol: 'FROM metrics. The table is called metrics.'" }
  ],
  "success": {
    "speaker": "carol",
    "text": "Right. Minute, service, n, errors. Every minute, every service. That's what we have."
  }
}
```

- [ ] **Step 4: Create `content/chapters/09-heat/puzzles/02.json`**

```json
{
  "id": "02",
  "concept": "rate",
  "brief": {
    "speaker": "carol",
    "text": "n is a count per minute. I want events per second. rate(n, 1m) converts it. Show me the top twenty by rate."
  },
  "template": [
    { "type": "keyword", "text": "SELECT" },
    { "type": "text",    "text": "minute, service, rate(" },
    { "type": "blank",   "id": "field", "mode": "typed", "placeholder": "field" },
    { "type": "text",    "text": ", 1m) AS rps" },
    { "type": "keyword", "text": "FROM" },
    { "type": "text",    "text": "metrics" },
    { "type": "keyword", "text": "ORDER BY" },
    { "type": "text",    "text": "rps DESC" },
    { "type": "keyword", "text": "LIMIT" },
    { "type": "text",    "text": "20" }
  ],
  "expected": {
    "method": "rows",
    "sql": "SELECT minute, service, ROUND(n / 60.0, 2) AS rps FROM metrics ORDER BY rps DESC LIMIT 20",
    "order_sensitive": true
  },
  "hints": [
    { "when": "wrong_count_high", "text": "Carol: 'Too many rows. LIMIT 20 is already in the query.'" },
    { "when": "wrong_count_low",  "text": "Carol: 'Not enough rows. The field should be n.'" },
    { "when": "error",            "text": "Carol: 'Query error. The field inside rate() is n — just the column name, no quotes.'" },
    { "when": "different_values", "text": "Carol: 'Wrong values. The field is n — total events per minute.'" },
    { "when": "default",          "text": "Carol: 'rate(n, 1m). n is the count column. 1m matches the bucket size.'" }
  ],
  "success": {
    "speaker": "carol",
    "text": "That's the unit I can think in. Most services running at fractions of a second. Some minutes running hotter."
  }
}
```

- [ ] **Step 5: Create `content/chapters/09-heat/puzzles/03.json`**

```json
{
  "id": "03",
  "concept": "rate",
  "brief": {
    "speaker": "carol",
    "text": "Which service peaks highest? One number per service — the maximum rate any single minute hit."
  },
  "template": [
    { "type": "keyword", "text": "SELECT" },
    { "type": "text",    "text": "service, MAX(rate(n, 1m)) AS peak_rps" },
    { "type": "keyword", "text": "FROM" },
    { "type": "text",    "text": "metrics" },
    { "type": "keyword", "text": "GROUP BY" },
    { "type": "blank",   "id": "group_col", "mode": "typed", "placeholder": "column" },
    { "type": "keyword", "text": "ORDER BY" },
    { "type": "text",    "text": "peak_rps DESC" }
  ],
  "expected": {
    "method": "rows",
    "sql": "SELECT service, MAX(ROUND(n / 60.0, 2)) AS peak_rps FROM metrics GROUP BY service ORDER BY peak_rps DESC",
    "order_sensitive": true
  },
  "hints": [
    { "when": "wrong_count_high", "text": "Carol: 'Too many rows. GROUP BY service — one row per service.'" },
    { "when": "wrong_count_low",  "text": "Carol: 'Not enough rows. GROUP BY service.'" },
    { "when": "error",            "text": "Carol: 'Query error. GROUP BY service — that column is called service.'" },
    { "when": "different_values", "text": "Carol: 'Wrong values. Group by service.'" },
    { "when": "default",          "text": "Carol: 'GROUP BY service. One row per service, highest rate for that service.'" }
  ],
  "success": {
    "speaker": "carol",
    "text": "There it is. chrono-portal-mirror at the top by a wide margin. Everything else is noise."
  }
}
```

- [ ] **Step 6: Create `content/chapters/09-heat/puzzles/04.json`**

```json
{
  "id": "04",
  "concept": "rate",
  "brief": {
    "speaker": "carol",
    "text": "Zoom in. Show me every minute for chrono-portal-mirror. Rate and errors both."
  },
  "template": [
    { "type": "keyword", "text": "SELECT" },
    { "type": "text",    "text": "minute, rate(n, 1m) AS rps, rate(" },
    { "type": "blank",   "id": "err_field", "mode": "typed", "placeholder": "field" },
    { "type": "text",    "text": ", 1m) AS err_rps" },
    { "type": "keyword", "text": "FROM" },
    { "type": "text",    "text": "metrics" },
    { "type": "keyword", "text": "WHERE" },
    { "type": "text",    "text": "service = 'chrono-portal-mirror'" },
    { "type": "keyword", "text": "ORDER BY" },
    { "type": "text",    "text": "minute" }
  ],
  "expected": {
    "method": "rows",
    "sql": "SELECT minute, ROUND(n / 60.0, 2) AS rps, ROUND(errors / 60.0, 2) AS err_rps FROM metrics WHERE service = 'chrono-portal-mirror' ORDER BY minute",
    "order_sensitive": true
  },
  "hints": [
    { "when": "wrong_count_high", "text": "Carol: 'Too many rows. The WHERE clause is already filtering to chrono-portal-mirror.'" },
    { "when": "wrong_count_low",  "text": "Carol: 'Not enough rows. The error field is errors.'" },
    { "when": "error",            "text": "Carol: 'Query error. The second rate() field is errors — the error count column.'" },
    { "when": "different_values", "text": "Carol: 'Wrong values. The error field is errors.'" },
    { "when": "default",          "text": "Carol: 'rate(errors, 1m). The errors column is the count of error events that minute.'" }
  ],
  "success": {
    "speaker": "carol",
    "text": "Six spikes. The rest is flat. Whatever this thing is, it doesn't run continuously — it fires and goes quiet."
  }
}
```

- [ ] **Step 7: Create `content/chapters/09-heat/puzzles/05.json`**

```json
{
  "id": "05",
  "concept": "rate",
  "brief": {
    "speaker": "carol",
    "text": "Filter to just the hot minutes. Above one event per second."
  },
  "template": [
    { "type": "keyword", "text": "SELECT" },
    { "type": "text",    "text": "minute, rate(n, 1m) AS rps" },
    { "type": "keyword", "text": "FROM" },
    { "type": "text",    "text": "metrics" },
    { "type": "keyword", "text": "WHERE" },
    { "type": "text",    "text": "service = 'chrono-portal-mirror' AND rate(n, 1m) >" },
    { "type": "blank",   "id": "threshold", "mode": "typed", "placeholder": "threshold" },
    { "type": "keyword", "text": "ORDER BY" },
    { "type": "text",    "text": "minute" }
  ],
  "expected": {
    "method": "rows",
    "sql": "SELECT minute, ROUND(n / 60.0, 2) AS rps FROM metrics WHERE service = 'chrono-portal-mirror' AND ROUND(n / 60.0, 2) > 1.0 ORDER BY minute",
    "order_sensitive": true
  },
  "hints": [
    { "when": "wrong_count_high", "text": "Carol: 'Too many rows. The threshold should be above the quiet baseline — try 1.0.'" },
    { "when": "wrong_count_low",  "text": "Carol: 'No rows. The threshold is too high — try 1.0.'" },
    { "when": "error",            "text": "Carol: 'Query error. The threshold is a number — 1.0.'" },
    { "when": "default",          "text": "Carol: 'rate(n, 1m) > 1.0 — anything above one event per second.'" }
  ],
  "success": {
    "speaker": "carol",
    "text": "Six rows. Eighteen minutes apart, give or take. That's not noise. That's a schedule."
  }
}
```

- [ ] **Step 8: Create `content/chapters/09-heat/puzzles/06.json`**

```json
{
  "id": "06",
  "concept": "rate",
  "brief": {
    "speaker": "carol",
    "text": "During those bursts — what fraction are errors? Show me both rates for the hot minutes."
  },
  "template": [
    { "type": "keyword", "text": "SELECT" },
    { "type": "text",    "text": "minute, rate(n, 1m) AS rps, rate(" },
    { "type": "blank",   "id": "err_field", "mode": "typed", "placeholder": "field" },
    { "type": "text",    "text": ", 1m) AS err_rps" },
    { "type": "keyword", "text": "FROM" },
    { "type": "text",    "text": "metrics" },
    { "type": "keyword", "text": "WHERE" },
    { "type": "text",    "text": "service = 'chrono-portal-mirror' AND rate(n, 1m) > 1.0" },
    { "type": "keyword", "text": "ORDER BY" },
    { "type": "text",    "text": "minute" }
  ],
  "expected": {
    "method": "rows",
    "sql": "SELECT minute, ROUND(n / 60.0, 2) AS rps, ROUND(errors / 60.0, 2) AS err_rps FROM metrics WHERE service = 'chrono-portal-mirror' AND ROUND(n / 60.0, 2) > 1.0 ORDER BY minute",
    "order_sensitive": true
  },
  "hints": [
    { "when": "wrong_count_high", "text": "Carol: 'Too many rows. The WHERE clause filters to hot minutes only.'" },
    { "when": "wrong_count_low",  "text": "Carol: 'Not enough rows. The error field is errors.'" },
    { "when": "error",            "text": "Carol: 'Query error. The field inside the second rate() is errors.'" },
    { "when": "different_values", "text": "Carol: 'Wrong values. The error field is errors — the error count column.'" },
    { "when": "default",          "text": "Carol: 'rate(errors, 1m). Same pattern as rps but for the errors column.'" }
  ],
  "success": {
    "speaker": "carol",
    "text": "Every burst has errors. Not some — every single one. This thing is trying to do something and partially failing every time it fires."
  }
}
```

- [ ] **Step 9: Run content validation**

```bash
npm run validate-content
```

Expected: all chapters pass, including `09-heat` with 6 puzzles

- [ ] **Step 10: Commit**

```bash
git add content/chapters/09-heat/
git commit -m "feat(ch9): add seed, chapter, and puzzle content files"
```

---

## Task 4: Reference doc + engine wiring

**Files:**
- Create: `content/reference/rate.md`
- Modify: `src/main.js:9`
- Modify: `src/reference.js`

- [ ] **Step 1: Create `content/reference/rate.md`**

```markdown
---
concept: rate
title: rate()
introduced_in: 09-heat
---

# rate()

`rate(field, 1m)` converts a per-minute count into events per second.

```sql
SELECT minute, service, rate(n, 1m) AS rps
FROM metrics
ORDER BY rps DESC
```

The `1m` interval matches the bucket size of the data. Divides by 60 and rounds to two decimal places.

Use it anywhere a column appears: SELECT, WHERE, ORDER BY, inside aggregates like MAX or AVG.

| DDSQL | Equivalent |
|-------|------------|
| `rate(n, 1m)` | `ROUND(n / 60.0, 2)` |
| `rate(errors, 1m)` | `ROUND(errors / 60.0, 2)` |
```

- [ ] **Step 2: Append `'09-heat'` to `CHAPTER_ORDER` in `src/main.js`**

Change line 9 of `src/main.js`:
```js
// Before:
const CHAPTER_ORDER = ['01-onboarding', '02-pharaoh', '03-speakeasy', '04-census', '05-tavern', '06-reunion', '07-static', '08-when'];
// After:
const CHAPTER_ORDER = ['01-onboarding', '02-pharaoh', '03-speakeasy', '04-census', '05-tavern', '06-reunion', '07-static', '08-when', '09-heat'];
```

- [ ] **Step 3: Add `'09-heat'` entry to `src/reference.js`**

After the `'08-when'` entry (ending at line 87), add:

```js
  '09-heat': [
    { slug: 'select',               title: 'SELECT' },
    { slug: 'from',                 title: 'FROM' },
    { slug: 'where',                title: 'WHERE' },
    { slug: 'group-by',             title: 'GROUP BY' },
    { slug: 'order-by',             title: 'ORDER BY' },
    { slug: 'ddsql-tags',           title: 'DDSQL tags' },
    { slug: 'time-windows',         title: 'Time windows' },
    { slug: 'count',                title: 'COUNT' },
    { slug: 'rate',                 title: 'rate()' },
  ],
```

- [ ] **Step 4: Run full test suite**

```bash
npm test
```

Expected: all tests pass

- [ ] **Step 5: Commit**

```bash
git add content/reference/rate.md src/main.js src/reference.js
git commit -m "feat(ch9): wire rate reference, add chapter to engine"
```

---

## Task 5: E2E smoke test

**Files:**
- Modify: `tests/e2e-smoke.spec.js`

- [ ] **Step 1: Append Ch9 test to `tests/e2e-smoke.spec.js`**

```js
test('Chapter 9 typing puzzle can be solved', async ({ page }) => {
  await page.goto('/');
  await page.evaluate(() => localStorage.clear());
  await page.reload();

  await page.evaluate(() => {
    localStorage.setItem('chronoConsultingState-v1', JSON.stringify({
      currentChapterId: '09-heat',
      currentPuzzleId: '01',
      chapters: {
        '01-onboarding': { completed: true, solved: ['01','02','03','04','05'], attempts: {} },
        '02-pharaoh':    { completed: true, solved: ['01','02','03','04','05','06'], attempts: {} },
        '03-speakeasy':  { completed: true, solved: ['01','02','03','04','05','06'], attempts: {} },
        '04-census':     { completed: true, solved: ['01','02','03','04','05','06'], attempts: {} },
        '05-tavern':     { completed: true, solved: ['01','02','03','04','05','06'], attempts: {} },
        '06-reunion':    { completed: true, solved: ['01','02','03','04','05','06'], attempts: {} },
        '07-static':     { completed: true, solved: ['01','02','03','04','05','06'], attempts: {} },
        '08-when':       { completed: true, solved: ['01','02','03','04','05','06'], attempts: {} },
      },
      referenceOpened: [],
      savedAt: Date.now(),
    }));
  });
  await page.reload();

  await expect(page.locator('.bubble')).toHaveCount(2, { timeout: 5000 });
  await expect(page.locator('.bubble').last()).toContainText('counts by the minute');

  const inputs = page.locator('.typed-input');
  await expect(inputs).toHaveCount(1);

  await inputs.nth(0).fill('metrics');

  const runBtn = page.locator('#run-btn');
  await expect(runBtn).toBeEnabled();
  await runBtn.click();

  await expect(page.locator('.bubble.success')).toHaveCount(1, { timeout: 5000 });
});
```

- [ ] **Step 2: Run E2E tests**

```bash
npx playwright test tests/e2e-smoke.spec.js
```

Expected: all tests pass including the new Ch9 test

- [ ] **Step 3: Commit**

```bash
git add tests/e2e-smoke.spec.js
git commit -m "test(e2e): add Chapter 9 smoke test"
```

---

## Task 6: Playtest checklist

**Files:**
- Modify: `docs/playtest-checklist.md`

- [ ] **Step 1: Append Chapter 9 section to `docs/playtest-checklist.md`**

Append to the end of the file:

```markdown
## Chapter 9 — Heat (rate function)

### Boot & navigation
- [ ] Solving Chapter 8 Puzzle 06 auto-advances to Chapter 9; OR set localStorage state to land directly.
- [ ] Carol's boss intro renders, mentioning pre-aggregated metrics and the rate function.
- [ ] Progress indicator shows Chapter 9.

### Mechanic — typing (regression)
- [ ] All Chapter 9 puzzles render text inputs (not dropdowns, not word-bank chips).
- [ ] Run button is disabled until every blank has at least one character.

### Per-puzzle correctness
- [ ] **Puzzle 01:** Type `metrics`. Submit. Ten rows returned with columns minute, service, n, errors. Success bubble appears.
- [ ] **Puzzle 01 wrong path:** Type `logs`. Error hint fires.
- [ ] **Puzzle 02:** Type `n`. Submit. 20 rows ordered by rps DESC. chrono-portal-mirror spike minutes appear near the top. Success bubble.
- [ ] **Puzzle 02 wrong path:** Type `errors`. Different values hint fires.
- [ ] **Puzzle 03:** Type `service`. Submit. 6 rows, chrono-portal-mirror first with peak_rps ≈ 3.x. Success bubble.
- [ ] **Puzzle 04:** Type `errors`. Submit. 120 rows for chrono-portal-mirror, ordered by minute. Both rps and err_rps columns visible. Success bubble.
- [ ] **Puzzle 05:** Type `1.0`. Submit. Exactly 6 rows. Success bubble mentions schedule.
- [ ] **Puzzle 05 wrong path:** Type `3.5`. Zero rows. wrong_count_low hint fires.
- [ ] **Puzzle 06:** Type `errors`. Submit. 6 rows with both rps and err_rps. Success bubble says "every single one."
- [ ] **Puzzle 06 wrong path:** Type `n`. Different values hint fires (same rows, wrong second column).

### Outro
- [ ] After solving Puzzle 06, outro fires.
- [ ] Carol's outro mentions "eighteen minutes" and "schedule."
- [ ] Outro does NOT auto-advance (Chapter 10 not yet shipped).

### Reference drawer — Chapter 9
- [ ] Drawer shows 9 concepts: SELECT, FROM, WHERE, GROUP BY, ORDER BY, DDSQL tags, Time windows, COUNT, rate().
- [ ] rate() entry renders markdown without error, shows the DDSQL → equivalent table.
- [ ] Switching between concepts works; aria-current updates.
```

- [ ] **Step 2: Commit**

```bash
git add docs/playtest-checklist.md
git commit -m "docs: add Chapter 9 playtest checklist section"
```
