# Datadog Agent — collect chrono logs + APM
logs:
  - type: journald
    source: chrono
    service: chrono-consulting
    include_units:
      - chrono.service
apm_config:
  enabled: true
  apm_non_local_traffic: false
